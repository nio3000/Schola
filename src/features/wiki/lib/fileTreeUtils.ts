/**
 * Shared file-tree utilities for wikilink resolution and index building.
 *
 * Re-exports from src/lib/fileTreeUtils.ts for backward compatibility.
 */

export {
  flattenFiles,
  isMarkdownFile,
  stripExtension,
  getMarkdownPaths,
} from '../../../lib/fileTreeUtils';
